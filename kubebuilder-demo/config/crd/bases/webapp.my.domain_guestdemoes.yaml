---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: guestdemoes.webapp.my.domain
spec:
  group: webapp.my.domain
  names:
    kind: Guestdemo
    listKind: GuestdemoList
    plural: guestdemoes
    singular: guestdemo
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Guestdemo is the Schema for the guestdemoes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GuestdemoSpec defines the desired state of Guestdemo.
            properties:
              num:
                type: integer
              port:
                maximum: 7000
                minimum: 6000
                type: integer
            type: object
          status:
            description: GuestdemoStatus defines the observed state of Guestdemo.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
